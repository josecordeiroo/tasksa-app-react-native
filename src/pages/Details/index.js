import React from 'react';
import { View, Text } from 'react-native';

export default function Details (id, description) {
  return (
    <View>
        <Text>{description}</Text>
    </View>
   );
 }
